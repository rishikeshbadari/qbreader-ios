**Flow**: multiplayer host

**Repro**:
1. Start Metro with `EXPO_PUBLIC_USE_PAIRED_LOOPBACK=1`.
2. Go to Multiplayer -> Start a Game.
3. Start the lobby.

**Observed**: The lobby stayed on **Generating game code...** because local loopback transports never produced a Supabase room-created callback. The host also started the transport using the player ID instead of the generated game session ID.

**Expected**: Local playtest lobbies should immediately show a valid six-character game code, and host/join should use the same session ID.

**Suspected file**: `context/MultiplayerContext.tsx`

**Status**: fixed
